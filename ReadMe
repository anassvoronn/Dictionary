Configure the database in application.yml:
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Dictionary?useUnicode=yes&characterEncoding=UTF-8
    username: postgres
    password: vampyrrr9712

Run the application:
java -jar Dictionary-0.0.1-SNAPSHOT.jar

Open Swagger UI:
Go to http://localhost:8080/api/swagger-ui/index.html to explore the API.

Run the docker:
1. docker network create dictionary-network
2. cd .\Vampyrrr\Dictionary\
3. mvn clean install
4. docker run --name dictionary-postgres --network dictionary-network -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=vampyrrr9712 -e POSTGRES_DB=Dictionary -p 5432:5432 -d postgres:latest
5. docker build -t dictionary-app .
6. docker run --name pgadmin -p 5050:80 -e PGADMIN_DEFAULT_EMAIL=admin@example.com -e PGADMIN_DEFAULT_PASSWORD=admin123 --network dictionary-network -d dpage/pgadmin4
7. docker run --name dictionary-app --network dictionary-network -e SPRING_DATASOURCE_URL="jdbc:postgresql://dictionary-postgres:5432/Dictionary" -e SPRING_DATASOURCE_USERNAME=postgres -e SPRING_DATASOURCE_PASSWORD=vampyrrr9712 -p 8080:8080 -d dictionary-app
